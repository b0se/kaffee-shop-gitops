# Alternative: Headlamp NICHT nach aussen exponieren.
# Service bleibt ClusterIP, Zugriff nur per kubectl port-forward.
service:
  type: ClusterIP
  port: 80
resources:
  requests: { cpu: 50m, memory: 128Mi }
  limits:   { cpu: 500m, memory: 512Mi }
